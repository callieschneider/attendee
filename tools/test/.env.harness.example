# Harness env (gitignored). Copy this file's contents to .env.harness and
# fill in the values; the harness scripts auto-load it.

AGENT_DEBUG_TOKEN=
AGENT_BASE_URL=https://meeting-agent-web-production.up.railway.app
TEST_MEET_URL=
